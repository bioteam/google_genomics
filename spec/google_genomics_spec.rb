require 'spec_helper'

describe GoogleGenomics do
  it 'has a version number' do
    expect(GoogleGenomics::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
